{% extends "base.twig" %}

{% block content %}
    <div class="l-wrapper u-p">
    <article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
        <h1>{{ post.title }}</h1>
        {{ post.html_content }}
    </article>
    </div>
{% endblock %}